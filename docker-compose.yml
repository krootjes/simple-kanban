services:
  kanban:
    build: .
    container_name: kanban
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - KANBAN_USERNAME=admin
      - KANBAN_PASSWORD=changeme
